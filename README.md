# anipler

Automated torrent file transfer system with Telegram bot control.

## Build

Use `zigbuild` to build distributable binaries:

```
cargo zigbuild --target x86_64-unknown-linux-musl --release
```
